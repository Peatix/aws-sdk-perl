{
   "schema" : 1,
   "perl_version" : "5.36.0",
   "timestamp" : null,
   "results" : {
      "startup-cold" : {
         "wallclock_seconds_median" : null,
         "iterations" : 5,
         "_note" : "first capture lands via .github/workflows/benchmark-capture.yml after stack17 merges"
      },
      "decode-warm-2500" : {
         "wallclock_seconds_median" : null,
         "iterations" : 5,
         "_note" : "first capture lands via .github/workflows/benchmark-capture.yml after stack17 merges"
      }
   },
   "_note" : "Skeleton populated by stack17. Real numbers land via the auto-PR opened by .github/workflows/benchmark-capture.yml after the next push to master. Until that auto-PR lands the strict gate (.github/workflows/benchmarks.yml --strict) treats null medians as 'no comparison possible' and passes informationally."
}

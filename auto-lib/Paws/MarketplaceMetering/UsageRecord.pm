# Generated by default/object.tt
package Paws::MarketplaceMetering::UsageRecord;
  use Moose;
  has CustomerAWSAccountId => (is => 'ro', isa => 'Str');
  has CustomerIdentifier => (is => 'ro', isa => 'Str');
  has Dimension => (is => 'ro', isa => 'Str', required => 1);
  has Quantity => (is => 'ro', isa => 'Int');
  has Timestamp => (is => 'ro', isa => 'Str', required => 1);
  has UsageAllocations => (is => 'ro', isa => 'ArrayRef[Paws::MarketplaceMetering::UsageAllocation]');

1;

### main pod documentation begin ###

=head1 NAME

Paws::MarketplaceMetering::UsageRecord

=head1 USAGE

This class represents one of two things:

=head3 Arguments in a call to a service

Use the attributes of this class as arguments to methods. You shouldn't make instances of this class. 
Each attribute should be used as a named argument in the calls that expect this type of object.

As an example, if Att1 is expected to be a Paws::MarketplaceMetering::UsageRecord object:

  $service_obj->Method(Att1 => { CustomerAWSAccountId => $value, ..., UsageAllocations => $value  });

=head3 Results returned from an API call

Use accessors for each attribute. If Att1 is expected to be an Paws::MarketplaceMetering::UsageRecord object:

  $result = $service_obj->Method(...);
  $result->Att1->CustomerAWSAccountId

=head1 DESCRIPTION

A C<UsageRecord> indicates a quantity of usage for a given product,
customer, dimension and time.

Multiple requests with the same C<UsageRecords> as input will be
de-duplicated to prevent double charges.

=head1 ATTRIBUTES


=head2 CustomerAWSAccountId => Str

The C<CustomerAWSAccountID> parameter specifies the AWS account ID of
the buyer.


=head2 CustomerIdentifier => Str

The C<CustomerIdentifier> is obtained through the C<ResolveCustomer>
operation and represents an individual buyer in your application.


=head2 B<REQUIRED> Dimension => Str

During the process of registering a product on Amazon Web Services
Marketplace, dimensions are specified. These represent different units
of value in your application.


=head2 Quantity => Int

The quantity of usage consumed by the customer for the given dimension
and time. Defaults to C<0> if not specified.


=head2 B<REQUIRED> Timestamp => Str

Timestamp, in UTC, for which the usage is being reported.

Your application can meter usage for up to one hour in the past. Make
sure the C<timestamp> value is not before the start of the software
usage.


=head2 UsageAllocations => ArrayRef[L<Paws::MarketplaceMetering::UsageAllocation>]

The set of C<UsageAllocations> to submit. The sum of all
C<UsageAllocation> quantities must equal the Quantity of the
C<UsageRecord>.



=head1 SEE ALSO

This class forms part of L<Paws>, describing an object used in L<Paws::MarketplaceMetering>

=head1 BUGS and CONTRIBUTIONS

The source code is located here: L<https://github.com/pplu/aws-sdk-perl>

Please report bugs to: L<https://github.com/pplu/aws-sdk-perl/issues>

=cut


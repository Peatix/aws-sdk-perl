# Generated by default/object.tt
package Paws::MailManager::SnsAction;
  use Moose;
  has ActionFailurePolicy => (is => 'ro', isa => 'Str');
  has Encoding => (is => 'ro', isa => 'Str');
  has PayloadType => (is => 'ro', isa => 'Str');
  has RoleArn => (is => 'ro', isa => 'Str', required => 1);
  has TopicArn => (is => 'ro', isa => 'Str', required => 1);

1;

### main pod documentation begin ###

=head1 NAME

Paws::MailManager::SnsAction

=head1 USAGE

This class represents one of two things:

=head3 Arguments in a call to a service

Use the attributes of this class as arguments to methods. You shouldn't make instances of this class. 
Each attribute should be used as a named argument in the calls that expect this type of object.

As an example, if Att1 is expected to be a Paws::MailManager::SnsAction object:

  $service_obj->Method(Att1 => { ActionFailurePolicy => $value, ..., TopicArn => $value  });

=head3 Results returned from an API call

Use accessors for each attribute. If Att1 is expected to be an Paws::MailManager::SnsAction object:

  $result = $service_obj->Method(...);
  $result->Att1->ActionFailurePolicy

=head1 DESCRIPTION

The action to publish the email content to an Amazon SNS topic. When
executed, this action will send the email as a notification to the
specified SNS topic.

=head1 ATTRIBUTES


=head2 ActionFailurePolicy => Str

A policy that states what to do in the case of failure. The action will
fail if there are configuration errors. For example, specified SNS
topic has been deleted or the role lacks necessary permissions to call
the C<sns:Publish> API.


=head2 Encoding => Str

The encoding to use for the email within the Amazon SNS notification.
The default value is C<UTF-8>. Use C<BASE64> if you need to preserve
all special characters, especially when the original message uses a
different encoding format.


=head2 PayloadType => Str

The expected payload type within the Amazon SNS notification.
C<CONTENT> attempts to publish the full email content with 20KB of
headers content. C<HEADERS> extracts up to 100KB of header content to
include in the notification, email content will not be included to the
notification. The default value is C<CONTENT>.


=head2 B<REQUIRED> RoleArn => Str

The Amazon Resource Name (ARN) of the IAM Role to use while writing to
Amazon SNS. This role must have access to the C<sns:Publish> API for
the given topic.


=head2 B<REQUIRED> TopicArn => Str

The Amazon Resource Name (ARN) of the Amazon SNS Topic to which
notification for the email received will be published.



=head1 SEE ALSO

This class forms part of L<Paws>, describing an object used in L<Paws::MailManager>

=head1 BUGS and CONTRIBUTIONS

The source code is located here: L<https://github.com/pplu/aws-sdk-perl>

Please report bugs to: L<https://github.com/pplu/aws-sdk-perl/issues>

=cut


# Generated by default/object.tt
package Paws::CognitoIdp::PasswordPolicyType;
  use Moose;
  has MinimumLength => (is => 'ro', isa => 'Int');
  has PasswordHistorySize => (is => 'ro', isa => 'Int');
  has RequireLowercase => (is => 'ro', isa => 'Bool');
  has RequireNumbers => (is => 'ro', isa => 'Bool');
  has RequireSymbols => (is => 'ro', isa => 'Bool');
  has RequireUppercase => (is => 'ro', isa => 'Bool');
  has TemporaryPasswordValidityDays => (is => 'ro', isa => 'Int');

1;

### main pod documentation begin ###

=head1 NAME

Paws::CognitoIdp::PasswordPolicyType

=head1 USAGE

This class represents one of two things:

=head3 Arguments in a call to a service

Use the attributes of this class as arguments to methods. You shouldn't make instances of this class. 
Each attribute should be used as a named argument in the calls that expect this type of object.

As an example, if Att1 is expected to be a Paws::CognitoIdp::PasswordPolicyType object:

  $service_obj->Method(Att1 => { MinimumLength => $value, ..., TemporaryPasswordValidityDays => $value  });

=head3 Results returned from an API call

Use accessors for each attribute. If Att1 is expected to be an Paws::CognitoIdp::PasswordPolicyType object:

  $result = $service_obj->Method(...);
  $result->Att1->MinimumLength

=head1 DESCRIPTION

The password policy settings for a user pool, including complexity,
history, and length requirements.

=head1 ATTRIBUTES


=head2 MinimumLength => Int

The minimum length of the password in the policy that you have set.
This value can't be less than 6.


=head2 PasswordHistorySize => Int

The number of previous passwords that you want Amazon Cognito to
restrict each user from reusing. Users can't set a password that
matches any of C<n> previous passwords, where C<n> is the value of
C<PasswordHistorySize>.


=head2 RequireLowercase => Bool

The requirement in a password policy that users must include at least
one lowercase letter in their password.


=head2 RequireNumbers => Bool

The requirement in a password policy that users must include at least
one number in their password.


=head2 RequireSymbols => Bool

The requirement in a password policy that users must include at least
one symbol in their password.


=head2 RequireUppercase => Bool

The requirement in a password policy that users must include at least
one uppercase letter in their password.


=head2 TemporaryPasswordValidityDays => Int

The number of days a temporary password is valid in the password
policy. If the user doesn't sign in during this time, an administrator
must reset their password. Defaults to C<7>. If you submit a value of
C<0>, Amazon Cognito treats it as a null value and sets
C<TemporaryPasswordValidityDays> to its default value.

When you set C<TemporaryPasswordValidityDays> for a user pool, you can
no longer set a value for the legacy C<UnusedAccountValidityDays>
parameter in that user pool.



=head1 SEE ALSO

This class forms part of L<Paws>, describing an object used in L<Paws::CognitoIdp>

=head1 BUGS and CONTRIBUTIONS

The source code is located here: L<https://github.com/pplu/aws-sdk-perl>

Please report bugs to: L<https://github.com/pplu/aws-sdk-perl/issues>

=cut


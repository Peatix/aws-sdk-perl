# Generated by default/object.tt
package Paws::CognitoIdp::WebAuthnConfigurationType;
  use Moose;
  has RelyingPartyId => (is => 'ro', isa => 'Str');
  has UserVerification => (is => 'ro', isa => 'Str');

1;

### main pod documentation begin ###

=head1 NAME

Paws::CognitoIdp::WebAuthnConfigurationType

=head1 USAGE

This class represents one of two things:

=head3 Arguments in a call to a service

Use the attributes of this class as arguments to methods. You shouldn't make instances of this class. 
Each attribute should be used as a named argument in the calls that expect this type of object.

As an example, if Att1 is expected to be a Paws::CognitoIdp::WebAuthnConfigurationType object:

  $service_obj->Method(Att1 => { RelyingPartyId => $value, ..., UserVerification => $value  });

=head3 Results returned from an API call

Use accessors for each attribute. If Att1 is expected to be an Paws::CognitoIdp::WebAuthnConfigurationType object:

  $result = $service_obj->Method(...);
  $result->Att1->RelyingPartyId

=head1 DESCRIPTION

Settings for authentication (MFA) with passkey, or webauthN, biometric
and security-key devices in a user pool. Configures the following:

=over

=item *

Configuration for requiring user-verification support in passkeys.

=item *

The user pool relying-party ID. This is the domain, typically your user
pool domain, that user's passkey providers should trust as a receiver
of passkey authentication.

=item *

The providers that you want to allow as origins for passkey
authentication.

=back


=head1 ATTRIBUTES


=head2 RelyingPartyId => Str

Sets or displays the authentication domain, typically your user pool
domain, that passkey providers must use as a relying party (RP) in
their configuration.

Under the following conditions, the passkey relying party ID must be
the fully-qualified domain name of your custom domain:

=over

=item *

The user pool is configured for passkey authentication.

=item *

The user pool has a custom domain, whether or not it also has a prefix
domain.

=item *

Your application performs authentication with managed login or the
classic hosted UI.

=back



=head2 UserVerification => Str

When C<required>, users can only register and sign in users with
passkeys that are capable of user verification
(https://www.w3.org/TR/webauthn-2/#enum-userVerificationRequirement).
When C<preferred>, your user pool doesn't require the use of
authenticators with user verification but encourages it.



=head1 SEE ALSO

This class forms part of L<Paws>, describing an object used in L<Paws::CognitoIdp>

=head1 BUGS and CONTRIBUTIONS

The source code is located here: L<https://github.com/pplu/aws-sdk-perl>

Please report bugs to: L<https://github.com/pplu/aws-sdk-perl/issues>

=cut


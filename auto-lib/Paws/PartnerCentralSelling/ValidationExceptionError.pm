# Generated by default/object.tt
package Paws::PartnerCentralSelling::ValidationExceptionError;
  use Moose;
  has Code => (is => 'ro', isa => 'Str', required => 1);
  has FieldName => (is => 'ro', isa => 'Str');
  has Message => (is => 'ro', isa => 'Str', required => 1);

1;

### main pod documentation begin ###

=head1 NAME

Paws::PartnerCentralSelling::ValidationExceptionError

=head1 USAGE

This class represents one of two things:

=head3 Arguments in a call to a service

Use the attributes of this class as arguments to methods. You shouldn't make instances of this class. 
Each attribute should be used as a named argument in the calls that expect this type of object.

As an example, if Att1 is expected to be a Paws::PartnerCentralSelling::ValidationExceptionError object:

  $service_obj->Method(Att1 => { Code => $value, ..., Message => $value  });

=head3 Results returned from an API call

Use accessors for each attribute. If Att1 is expected to be an Paws::PartnerCentralSelling::ValidationExceptionError object:

  $result = $service_obj->Method(...);
  $result->Att1->Code

=head1 DESCRIPTION

Indicates an invalid value for a field.

=over

=item *

I<REQUIRED_FIELD_MISSING:> The request is missing a required field.

Fix: Verify your request payload includes all required fields.

=item *

I<INVALID_ENUM_VALUE:> The enum field value isn't an accepted values.

Fix: Check the documentation for the list of valid enum values, and
update your request with a valid value.

=item *

I<INVALID_STRING_FORMAT:> The string format is invalid.

Fix: Confirm that the string is in the expected format (For example:
email address, date).

=item *

I<INVALID_VALUE:> The value isn't valid.

Fix: Confirm that the value meets the expected criteria and is within
the allowable range or set.

=item *

I<TOO_MANY_VALUES:> There are too many values in a field that expects
fewer entries.

Fix: Reduce the number of values to match the expected limit.

=item *

I<ACTION_NOT_PERMITTED:> The action isn't permitted due to current
state or permissions.

Fix: Verify that the action is appropriate for the current state, and
that you have the necessary permissions to perform it.

=item *

I<DUPLICATE_KEY_VALUE:> The value in a field duplicates a value that
must be unique.

Fix: Verify that the value is unique and doesn't duplicate an existing
value in the system.

=back


=head1 ATTRIBUTES


=head2 B<REQUIRED> Code => Str

Specifies the error code for the invalid field value.


=head2 FieldName => Str

Specifies the field name with the invalid value.


=head2 B<REQUIRED> Message => Str

Specifies the detailed error message for the invalid field value.



=head1 SEE ALSO

This class forms part of L<Paws>, describing an object used in L<Paws::PartnerCentralSelling>

=head1 BUGS and CONTRIBUTIONS

The source code is located here: L<https://github.com/pplu/aws-sdk-perl>

Please report bugs to: L<https://github.com/pplu/aws-sdk-perl/issues>

=cut


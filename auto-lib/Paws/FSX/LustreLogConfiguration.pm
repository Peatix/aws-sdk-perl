# Generated by default/object.tt
package Paws::FSX::LustreLogConfiguration;
  use Moose;
  has Destination => (is => 'ro', isa => 'Str');
  has Level => (is => 'ro', isa => 'Str', required => 1);

1;

### main pod documentation begin ###

=head1 NAME

Paws::FSX::LustreLogConfiguration

=head1 USAGE

This class represents one of two things:

=head3 Arguments in a call to a service

Use the attributes of this class as arguments to methods. You shouldn't make instances of this class. 
Each attribute should be used as a named argument in the calls that expect this type of object.

As an example, if Att1 is expected to be a Paws::FSX::LustreLogConfiguration object:

  $service_obj->Method(Att1 => { Destination => $value, ..., Level => $value  });

=head3 Results returned from an API call

Use accessors for each attribute. If Att1 is expected to be an Paws::FSX::LustreLogConfiguration object:

  $result = $service_obj->Method(...);
  $result->Att1->Destination

=head1 DESCRIPTION

The configuration for Lustre logging used to write the enabled logging
events for your Amazon FSx for Lustre file system or Amazon File Cache
resource to Amazon CloudWatch Logs.

=head1 ATTRIBUTES


=head2 Destination => Str

The Amazon Resource Name (ARN) that specifies the destination of the
logs. The destination can be any Amazon CloudWatch Logs log group ARN.
The destination ARN must be in the same Amazon Web Services partition,
Amazon Web Services Region, and Amazon Web Services account as your
Amazon FSx file system.


=head2 B<REQUIRED> Level => Str

The data repository events that are logged by Amazon FSx.

=over

=item *

C<WARN_ONLY> - only warning events are logged.

=item *

C<ERROR_ONLY> - only error events are logged.

=item *

C<WARN_ERROR> - both warning events and error events are logged.

=item *

C<DISABLED> - logging of data repository events is turned off.

=back

Note that Amazon File Cache uses a default setting of C<WARN_ERROR>,
which can't be changed.



=head1 SEE ALSO

This class forms part of L<Paws>, describing an object used in L<Paws::FSX>

=head1 BUGS and CONTRIBUTIONS

The source code is located here: L<https://github.com/pplu/aws-sdk-perl>

Please report bugs to: L<https://github.com/pplu/aws-sdk-perl/issues>

=cut


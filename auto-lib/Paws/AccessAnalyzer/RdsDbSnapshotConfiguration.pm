# Generated by default/object.tt
package Paws::AccessAnalyzer::RdsDbSnapshotConfiguration;
  use Moose;
  has Attributes => (is => 'ro', isa => 'Paws::AccessAnalyzer::RdsDbSnapshotAttributesMap', request_name => 'attributes', traits => ['NameInRequest']);
  has KmsKeyId => (is => 'ro', isa => 'Str', request_name => 'kmsKeyId', traits => ['NameInRequest']);

1;

### main pod documentation begin ###

=head1 NAME

Paws::AccessAnalyzer::RdsDbSnapshotConfiguration

=head1 USAGE

This class represents one of two things:

=head3 Arguments in a call to a service

Use the attributes of this class as arguments to methods. You shouldn't make instances of this class. 
Each attribute should be used as a named argument in the calls that expect this type of object.

As an example, if Att1 is expected to be a Paws::AccessAnalyzer::RdsDbSnapshotConfiguration object:

  $service_obj->Method(Att1 => { Attributes => $value, ..., KmsKeyId => $value  });

=head3 Results returned from an API call

Use accessors for each attribute. If Att1 is expected to be an Paws::AccessAnalyzer::RdsDbSnapshotConfiguration object:

  $result = $service_obj->Method(...);
  $result->Att1->Attributes

=head1 DESCRIPTION

The proposed access control configuration for an Amazon RDS DB
snapshot. You can propose a configuration for a new Amazon RDS DB
snapshot or an Amazon RDS DB snapshot that you own by specifying the
C<RdsDbSnapshotAttributeValue> and optional KMS encryption key. For
more information, see ModifyDBSnapshotAttribute
(https://docs.aws.amazon.com/AmazonRDS/latest/APIReference/API_ModifyDBSnapshotAttribute.html).

=head1 ATTRIBUTES


=head2 Attributes => L<Paws::AccessAnalyzer::RdsDbSnapshotAttributesMap>

The names and values of manual DB snapshot attributes. Manual DB
snapshot attributes are used to authorize other Amazon Web Services
accounts to restore a manual DB snapshot. The only valid value for
C<attributeName> for the attribute map is restore.


=head2 KmsKeyId => Str

The KMS key identifier for an encrypted Amazon RDS DB snapshot. The KMS
key identifier is the key ARN, key ID, alias ARN, or alias name for the
KMS key.

=over

=item *

If the configuration is for an existing Amazon RDS DB snapshot and you
do not specify the C<kmsKeyId>, or you specify an empty string, then
the access preview uses the existing C<kmsKeyId> of the snapshot.

=item *

If the access preview is for a new resource and you do not specify the
specify the C<kmsKeyId>, then the access preview considers the snapshot
as unencrypted.

=back




=head1 SEE ALSO

This class forms part of L<Paws>, describing an object used in L<Paws::AccessAnalyzer>

=head1 BUGS and CONTRIBUTIONS

The source code is located here: L<https://github.com/pplu/aws-sdk-perl>

Please report bugs to: L<https://github.com/pplu/aws-sdk-perl/issues>

=cut


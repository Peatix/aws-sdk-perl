# Generated by default/object.tt
package Paws::QConnect::EmailMessageTemplateContentBody;
  use Moose;
  has Html => (is => 'ro', isa => 'Paws::QConnect::MessageTemplateBodyContentProvider', request_name => 'html', traits => ['NameInRequest']);
  has PlainText => (is => 'ro', isa => 'Paws::QConnect::MessageTemplateBodyContentProvider', request_name => 'plainText', traits => ['NameInRequest']);

1;

### main pod documentation begin ###

=head1 NAME

Paws::QConnect::EmailMessageTemplateContentBody

=head1 USAGE

This class represents one of two things:

=head3 Arguments in a call to a service

Use the attributes of this class as arguments to methods. You shouldn't make instances of this class. 
Each attribute should be used as a named argument in the calls that expect this type of object.

As an example, if Att1 is expected to be a Paws::QConnect::EmailMessageTemplateContentBody object:

  $service_obj->Method(Att1 => { Html => $value, ..., PlainText => $value  });

=head3 Results returned from an API call

Use accessors for each attribute. If Att1 is expected to be an Paws::QConnect::EmailMessageTemplateContentBody object:

  $result = $service_obj->Method(...);
  $result->Att1->Html

=head1 DESCRIPTION

The body to use in email messages.

=head1 ATTRIBUTES


=head2 Html => L<Paws::QConnect::MessageTemplateBodyContentProvider>

The message body, in HTML format, to use in email messages that are
based on the message template. We recommend using HTML format for email
clients that render HTML content. You can include links, formatted
text, and more in an HTML message.


=head2 PlainText => L<Paws::QConnect::MessageTemplateBodyContentProvider>

The message body, in plain text format, to use in email messages that
are based on the message template. We recommend using plain text format
for email clients that don't render HTML content and clients that are
connected to high-latency networks, such as mobile devices.



=head1 SEE ALSO

This class forms part of L<Paws>, describing an object used in L<Paws::QConnect>

=head1 BUGS and CONTRIBUTIONS

The source code is located here: L<https://github.com/pplu/aws-sdk-perl>

Please report bugs to: L<https://github.com/pplu/aws-sdk-perl/issues>

=cut


# Generated by default/object.tt
package Paws::WorkSpaces::IosClientBrandingAttributes;
  use Moose;
  has ForgotPasswordLink => (is => 'ro', isa => 'Str');
  has LoginMessage => (is => 'ro', isa => 'Paws::WorkSpaces::LoginMessage');
  has Logo2xUrl => (is => 'ro', isa => 'Str');
  has Logo3xUrl => (is => 'ro', isa => 'Str');
  has LogoUrl => (is => 'ro', isa => 'Str');
  has SupportEmail => (is => 'ro', isa => 'Str');
  has SupportLink => (is => 'ro', isa => 'Str');

1;

### main pod documentation begin ###

=head1 NAME

Paws::WorkSpaces::IosClientBrandingAttributes

=head1 USAGE

This class represents one of two things:

=head3 Arguments in a call to a service

Use the attributes of this class as arguments to methods. You shouldn't make instances of this class. 
Each attribute should be used as a named argument in the calls that expect this type of object.

As an example, if Att1 is expected to be a Paws::WorkSpaces::IosClientBrandingAttributes object:

  $service_obj->Method(Att1 => { ForgotPasswordLink => $value, ..., SupportLink => $value  });

=head3 Results returned from an API call

Use accessors for each attribute. If Att1 is expected to be an Paws::WorkSpaces::IosClientBrandingAttributes object:

  $result = $service_obj->Method(...);
  $result->Att1->ForgotPasswordLink

=head1 DESCRIPTION

The client branding attributes for iOS device types. These attributes
are displayed on the iOS client login screen only.

Client branding attributes are public facing. Ensure you do not include
sensitive information.

=head1 ATTRIBUTES


=head2 ForgotPasswordLink => Str

The forgotten password link. This is the web address that users can go
to if they forget the password for their WorkSpace.


=head2 LoginMessage => L<Paws::WorkSpaces::LoginMessage>

The login message. Specified as a key value pair, in which the key is a
locale and the value is the localized message for that locale. The only
key supported is C<en_US>. The HTML tags supported include the
following: C<a, b, blockquote, br, cite, code, dd, dl, dt, div, em, i,
li, ol, p, pre, q, small, span, strike, strong, sub, sup, u, ul>.


=head2 Logo2xUrl => Str

The @2x version of the logo. This is the higher resolution display that
offers a scale factor of 2.0 (or @2x). The only image format accepted
is a binary data object that is converted from a C<.png> file.

For more information about iOS image size and resolution, see Image
Size and Resolution
(https://developer.apple.com/design/human-interface-guidelines/ios/icons-and-images/image-size-and-resolution/)
in the I<Apple Human Interface Guidelines>.


=head2 Logo3xUrl => Str

The @3x version of the logo. This is the higher resolution display that
offers a scale factor of 3.0 (or @3x).The only image format accepted is
a binary data object that is converted from a C<.png> file.

For more information about iOS image size and resolution, see Image
Size and Resolution
(https://developer.apple.com/design/human-interface-guidelines/ios/icons-and-images/image-size-and-resolution/)
in the I<Apple Human Interface Guidelines>.


=head2 LogoUrl => Str

The logo. This is the standard-resolution display that has a 1:1 pixel
density (or @1x), where one pixel is equal to one point. The only image
format accepted is a binary data object that is converted from a
C<.png> file.


=head2 SupportEmail => Str

The support email. The company's customer support email address.

=over

=item *

In each platform type, the C<SupportEmail> and C<SupportLink>
parameters are mutually exclusive. You can specify one parameter for
each platform type, but not both.

=item *

The default email is C<workspaces-feedback@amazon.com>.

=back



=head2 SupportLink => Str

The support link. The link for the company's customer support page for
their WorkSpace.

=over

=item *

In each platform type, the C<SupportEmail> and C<SupportLink>
parameters are mutually exclusive. You can specify one parameter for
each platform type, but not both.

=item *

The default support link is C<workspaces-feedback@amazon.com>.

=back




=head1 SEE ALSO

This class forms part of L<Paws>, describing an object used in L<Paws::WorkSpaces>

=head1 BUGS and CONTRIBUTIONS

The source code is located here: L<https://github.com/pplu/aws-sdk-perl>

Please report bugs to: L<https://github.com/pplu/aws-sdk-perl/issues>

=cut


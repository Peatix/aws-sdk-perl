# Generated by default/object.tt
package Paws::ComprehendMedical::SNOMEDCTAttribute;
  use Moose;
  has BeginOffset => (is => 'ro', isa => 'Int');
  has Category => (is => 'ro', isa => 'Str');
  has EndOffset => (is => 'ro', isa => 'Int');
  has Id => (is => 'ro', isa => 'Int');
  has RelationshipScore => (is => 'ro', isa => 'Num');
  has RelationshipType => (is => 'ro', isa => 'Str');
  has Score => (is => 'ro', isa => 'Num');
  has SNOMEDCTConcepts => (is => 'ro', isa => 'ArrayRef[Paws::ComprehendMedical::SNOMEDCTConcept]');
  has Text => (is => 'ro', isa => 'Str');
  has Traits => (is => 'ro', isa => 'ArrayRef[Paws::ComprehendMedical::SNOMEDCTTrait]');
  has Type => (is => 'ro', isa => 'Str');

1;

### main pod documentation begin ###

=head1 NAME

Paws::ComprehendMedical::SNOMEDCTAttribute

=head1 USAGE

This class represents one of two things:

=head3 Arguments in a call to a service

Use the attributes of this class as arguments to methods. You shouldn't make instances of this class. 
Each attribute should be used as a named argument in the calls that expect this type of object.

As an example, if Att1 is expected to be a Paws::ComprehendMedical::SNOMEDCTAttribute object:

  $service_obj->Method(Att1 => { BeginOffset => $value, ..., Type => $value  });

=head3 Results returned from an API call

Use accessors for each attribute. If Att1 is expected to be an Paws::ComprehendMedical::SNOMEDCTAttribute object:

  $result = $service_obj->Method(...);
  $result->Att1->BeginOffset

=head1 DESCRIPTION

The extracted attributes that relate to an entity. An extracted segment
of the text that is an attribute of an entity, or otherwise related to
an entity, such as the dosage of a medication taken.

=head1 ATTRIBUTES


=head2 BeginOffset => Int

The 0-based character offset in the input text that shows where the
attribute begins. The offset returns the UTF-8 code point in the
string.


=head2 Category => Str

The category of the detected attribute. Possible categories include
MEDICAL_CONDITION, ANATOMY, and TEST_TREATMENT_PROCEDURE.


=head2 EndOffset => Int

The 0-based character offset in the input text that shows where the
attribute ends. The offset returns the UTF-8 code point in the string.


=head2 Id => Int

The numeric identifier for this attribute. This is a monotonically
increasing id unique within this response rather than a global unique
identifier.


=head2 RelationshipScore => Num

The level of confidence that Amazon Comprehend Medical has that this
attribute is correctly related to this entity.


=head2 RelationshipType => Str

The type of relationship that exists between the entity and the related
attribute.


=head2 Score => Num

The level of confidence that Amazon Comprehend Medical has that the
segment of text is correctly recognized as an attribute.


=head2 SNOMEDCTConcepts => ArrayRef[L<Paws::ComprehendMedical::SNOMEDCTConcept>]

The SNOMED-CT concepts specific to an attribute, along with a score
indicating the likelihood of the match.


=head2 Text => Str

The segment of input text extracted as this attribute.


=head2 Traits => ArrayRef[L<Paws::ComprehendMedical::SNOMEDCTTrait>]

Contextual information for an attribute. Examples include signs,
symptoms, diagnosis, and negation.


=head2 Type => Str

The type of attribute. Possible types include DX_NAME, ACUITY,
DIRECTION, SYSTEM_ORGAN_SITE,TEST_NAME, TEST_VALUE, TEST_UNIT,
PROCEDURE_NAME, and TREATMENT_NAME.



=head1 SEE ALSO

This class forms part of L<Paws>, describing an object used in L<Paws::ComprehendMedical>

=head1 BUGS and CONTRIBUTIONS

The source code is located here: L<https://github.com/pplu/aws-sdk-perl>

Please report bugs to: L<https://github.com/pplu/aws-sdk-perl/issues>

=cut


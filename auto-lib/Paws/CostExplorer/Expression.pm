# Generated by default/object.tt
package Paws::CostExplorer::Expression;
  use Moose;
  has And => (is => 'ro', isa => 'ArrayRef[Paws::CostExplorer::Expression]');
  has CostCategories => (is => 'ro', isa => 'Paws::CostExplorer::CostCategoryValues');
  has Dimensions => (is => 'ro', isa => 'Paws::CostExplorer::DimensionValues');
  has Not => (is => 'ro', isa => 'Paws::CostExplorer::Expression');
  has Or => (is => 'ro', isa => 'ArrayRef[Paws::CostExplorer::Expression]');
  has Tags => (is => 'ro', isa => 'Paws::CostExplorer::TagValues');

1;

### main pod documentation begin ###

=head1 NAME

Paws::CostExplorer::Expression

=head1 USAGE

This class represents one of two things:

=head3 Arguments in a call to a service

Use the attributes of this class as arguments to methods. You shouldn't make instances of this class. 
Each attribute should be used as a named argument in the calls that expect this type of object.

As an example, if Att1 is expected to be a Paws::CostExplorer::Expression object:

  $service_obj->Method(Att1 => { And => $value, ..., Tags => $value  });

=head3 Results returned from an API call

Use accessors for each attribute. If Att1 is expected to be an Paws::CostExplorer::Expression object:

  $result = $service_obj->Method(...);
  $result->Att1->And

=head1 DESCRIPTION

Use C<Expression> to filter in various Cost Explorer APIs.

Not all C<Expression> types are supported in each API. Refer to the
documentation for each specific API to see what is supported.

There are two patterns:

=over

=item *

Simple dimension values.

=over

=item *

There are three types of simple dimension values: C<CostCategories>,
C<Tags>, and C<Dimensions>.

=over

=item *

Specify the C<CostCategories> field to define a filter that acts on
Cost Categories.

=item *

Specify the C<Tags> field to define a filter that acts on Cost
Allocation Tags.

=item *

Specify the C<Dimensions> field to define a filter that acts on the
C<DimensionValues>
(https://docs.aws.amazon.com/aws-cost-management/latest/APIReference/API_DimensionValues.html).

=back

=item *

For each filter type, you can set the dimension name and values for the
filters that you plan to use.

=over

=item *

For example, you can filter for C<REGION==us-east-1 OR
REGION==us-west-1>. For C<GetRightsizingRecommendation>, the Region is
a full name (for example, C<REGION==US East (N. Virginia)>.

=item *

The corresponding C<Expression> for this example is as follows: C<{
"Dimensions": { "Key": "REGION", "Values": [ "us-east-1", "us-west-1" ]
} }>

=item *

As shown in the previous example, lists of dimension values are
combined with C<OR> when applying the filter.

=back

=item *

You can also set different match options to further control how the
filter behaves. Not all APIs support match options. Refer to the
documentation for each specific API to see what is supported.

=over

=item *

For example, you can filter for linked account names that start with
"a".

=item *

The corresponding C<Expression> for this example is as follows: C<{
"Dimensions": { "Key": "LINKED_ACCOUNT_NAME", "MatchOptions": [
"STARTS_WITH" ], "Values": [ "a" ] } }>

=back

=back

=item *

Compound C<Expression> types with logical operations.

=over

=item *

You can use multiple C<Expression> types and the logical operators
C<AND/OR/NOT> to create a list of one or more C<Expression> objects. By
doing this, you can filter by more advanced options.

=item *

For example, you can filter by C<((REGION == us-east-1 OR REGION ==
us-west-1) OR (TAG.Type == Type1)) AND (USAGE_TYPE != DataTransfer)>.

=item *

The corresponding C<Expression> for this example is as follows: C<{
"And": [ {"Or": [ {"Dimensions": { "Key": "REGION", "Values": [
"us-east-1", "us-west-1" ] }}, {"Tags": { "Key": "TagName", "Values":
["Value1"] } } ]}, {"Not": {"Dimensions": { "Key": "USAGE_TYPE",
"Values": ["DataTransfer"] }}} ] }>

=back

Because each C<Expression> can have only one operator, the service
returns an error if more than one is specified. The following example
shows an C<Expression> object that creates an error: C< { "And": [ ...
], "Dimensions": { "Key": "USAGE_TYPE", "Values": [ "DataTransfer" ] }
}>

The following is an example of the corresponding error message:
C<"Expression has more than one roots. Only one root operator is
allowed for each expression: And, Or, Not, Dimensions, Tags,
CostCategories">

=back

For the C<GetRightsizingRecommendation> action, a combination of OR and
NOT isn't supported. OR isn't supported between different dimensions,
or dimensions and tags. NOT operators aren't supported. Dimensions are
also limited to C<LINKED_ACCOUNT>, C<REGION>, or C<RIGHTSIZING_TYPE>.

For the C<GetReservationPurchaseRecommendation> action, only NOT is
supported. AND and OR aren't supported. Dimensions are limited to
C<LINKED_ACCOUNT>.

=head1 ATTRIBUTES


=head2 And => ArrayRef[L<Paws::CostExplorer::Expression>]

Return results that match both C<Dimension> objects.


=head2 CostCategories => L<Paws::CostExplorer::CostCategoryValues>

The filter that's based on C<CostCategory> values.


=head2 Dimensions => L<Paws::CostExplorer::DimensionValues>

The specific C<Dimension> to use for C<Expression>.


=head2 Not => L<Paws::CostExplorer::Expression>

Return results that don't match a C<Dimension> object.


=head2 Or => ArrayRef[L<Paws::CostExplorer::Expression>]

Return results that match either C<Dimension> object.


=head2 Tags => L<Paws::CostExplorer::TagValues>

The specific C<Tag> to use for C<Expression>.



=head1 SEE ALSO

This class forms part of L<Paws>, describing an object used in L<Paws::CostExplorer>

=head1 BUGS and CONTRIBUTIONS

The source code is located here: L<https://github.com/pplu/aws-sdk-perl>

Please report bugs to: L<https://github.com/pplu/aws-sdk-perl/issues>

=cut

